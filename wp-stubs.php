<?php
/**
 * Minimal WordPress function stubs for static analysis (Intelephense).
 * These are no-op implementations intended only to satisfy the language server.
 */

if (!function_exists('add_theme_support')) {
    function add_theme_support($feature, $args = null)
    {
        return null;
    }
}
